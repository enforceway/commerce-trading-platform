(($win: Window) => {
    $win['floatIntegrator'] = {
        show: () => {
            console.log('integrator open with wilde');
        },
        close: () => {
            console.log('integrator close with wilde');
        }
    };
})(window);
